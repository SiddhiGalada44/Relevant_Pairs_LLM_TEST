services:
  - type: web
    name: relevant-priors
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn --bind 0.0.0.0:$PORT --timeout 360 --workers 2 config.wsgi:application
    envVars:
      - key: GEMINI_API_KEY
        sync: false